{
    "name": "rapidwebltd/simple-currency-rates",
    "description": "This package provides simple access to current currency exchange rates.",
    "type": "library",
    "license": "LGPL-3.0-only",
    "authors": [
        {
            "name": "Jordan Hall",
            "email": "jordan.hall@rapidweb.biz"
        }
    ],
    "require": {
        "php": ">=5.6"
    },
    "require-dev": {
        "phpunit/phpunit": "^5.7 || ^9.6 || ^10.5 || ^11.5 || ^12.0",
        "psr/cache": "^1.0",
        "psr/simple-cache": "^1.0"
    },
    "suggest": {
        "psr/cache": "Use any PSR-6 cache pool instead of the built-in filesystem cache.",
        "psr/simple-cache": "Use any PSR-16 cache instead of the built-in filesystem cache."
    },
    "autoload": {
        "psr-4": {
            "RapidWeb\\SimpleCurrencyRates\\": "src/"
        }
    },
    "config": {
        "allow-plugins": {
            "kylekatarnls/update-helper": false
        }
    }
}
